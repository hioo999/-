{
  "name": "ip-system",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "deploy:test": "scripts/deploy-test.sh",
    "deploy:prod": "scripts/deploy-prod.sh",
    "harness:validate": "harness validate",
    "harness:check-deps": "harness check-deps"
  },
  "dependencies": {},
  "devDependencies": {
    "@harness-engineering/cli": "^2.7.1",
    "typescript": "^5.0.0"
  }
}
