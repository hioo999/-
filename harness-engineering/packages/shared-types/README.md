# shared-types

Shared TypeScript/Python-adjacent schema notes go here after API contracts are generated.

Rule:

```text
Platform shared types must not define case, file, chunk, vector, prompt, answer, or citation business payloads.
```
