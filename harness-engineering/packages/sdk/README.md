# sdk

API client scaffolding for Agent local APIs and platform control-plane APIs.

Boundary:

```text
Platform SDK exposes only control-plane APIs.
Agent SDK exposes local business APIs and must not be imported by platform-console.
```
