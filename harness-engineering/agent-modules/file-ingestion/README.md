# file-ingestion

Responsibilities:

```text
Local directory scan
File upload
Extension and MIME validation
Path traversal protection
Hash-based deduplication
local_files metadata persistence
processing_tasks creation
```
